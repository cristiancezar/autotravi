name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.3bc2206d-6bbc-44e7-91d1-d36ae0285919}}
          heroku_app_name: "autotravi" #Must be unique in Heroku
          heroku_email: "cristian.moises@autotravi.com"
           buildpack: "https://github.com/HashNuke/heroku-buildpack-elixir.git"
